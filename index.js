const searchBox = document.querySelector(['data-search']);

// add event listener to collect the input
searchBox.addEventListener('input', e => {
    const value = e.target.value
    console.log(value);
})

// create a search function and search the document
function search() {
    
}